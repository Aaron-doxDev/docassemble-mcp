# MCP Server Authentication
# Required: Bearer token for authenticating MCP clients
MCP_AUTH_TOKEN=your-secret-token-here

# Required: Comma-separated list of allowed origins for DNS rebinding protection
# Example: http://localhost:3000,https://claude.ai,https://app.devin.ai
MCP_ALLOWED_ORIGINS=http://localhost:3000

# Optional: Port to run the server on (default: 3000)
PORT=3000

# Optional: Path to the docassemble reference repository
# Default: ../docassemble-ref (relative to this project)
DOCASSEMBLE_REF_PATH=../docassemble-ref
